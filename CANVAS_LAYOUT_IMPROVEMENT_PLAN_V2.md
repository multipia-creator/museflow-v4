# Canvas V4 레이아웃 개선 계획 V2

## 📋 현재 상태 분석

### 현재 레이아웃 구조 (3-Column)
```
┌─────────────────────────────────────────────────────────────────┐
│  Navbar (64px)                                                  │
├──────────┬─────────────────────────┬─────────────────────────────┤
│ Left     │ Center (AI Chat)        │ Right (Results)             │
│ 300px    │ 1fr (가변)              │ 400px                       │
│          │                         │                             │
│ History  │ 🤖 AI Conversation      │ 📊 Overview                 │
│ Projects │    (너무 큼!)           │    Widgets                  │
│          │                         │    Results (중요!)          │
│          │                         │    (공간 부족!)             │
└──────────┴─────────────────────────┴─────────────────────────────┘
│  Footer (40px)                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 🔴 문제점
1. **AI Conversation 영역이 너무 큼**: Center 전체를 차지
2. **생성 결과 영역 부족**: Right 400px만 할당, 내용 표시 제한
3. **여백 과다**: 
   - padding: 1.5rem ~ 2rem
   - gap: 1rem ~ 1.5rem
   - margin: 1rem ~ 2rem
4. **무한 캔버스 부재**: 생성 결과를 자유롭게 배치/이동 불가
5. **정보 밀도 낮음**: 화면에 표시되는 정보량이 적음

---

## 🎯 개선 목표

### 우선순위
1. **생성 결과 영역 최대화** (가장 중요!)
2. **AI Chat 최소화** (접을 수 있게)
3. **무한 캔버스 구현** (드래그, 확대/축소, 복사)
4. **여백 최적화** (30-40% 축소)
5. **정보 밀도 증가** (50% 더 많은 내용 표시)

---

## 📐 새로운 레이아웃 제안

### Option A: 2열 + 무한 캔버스 (추천!)

```
┌─────────────────────────────────────────────────────────────────┐
│  Navbar (64px) - Dark Theme                                     │
├──────────┬──────────────────────────────────────────────────────┤
│ Left     │ Right - Infinite Canvas (무한 캔버스)                │
│ 280px    │ 1fr (80%+)                                           │
│ (축소)   │                                                      │
│          │  [생성된 이미지]  [텍스트 카드]  [차트]              │
│ History  │     ↓ 드래그       ↓ 복사        ↓ 이동             │
│          │                                                      │
│ AI Chat  │  [위젯 A]         [보고서 B]                        │
│ (접힘)   │     ↓ 크기조정      ↓ 확대/축소                      │
│ 48px     │                                                      │
│          │  Grid (20px), Zoom (25-300%)                         │
└──────────┴──────────────────────────────────────────────────────┘
│  Footer (40px) - Dark Theme                                     │
└─────────────────────────────────────────────────────────────────┘
```

**장점:**
- ✅ 생성 결과 영역 300% 증가 (400px → 80%+)
- ✅ AI Chat 접으면 48px → 화면 넓어짐
- ✅ 무한 캔버스로 자유로운 작업
- ✅ 드래그 앤 드롭으로 직관적 조작
- ✅ 현재 구조에서 쉽게 전환 가능

---

### Option B: 탭 기반 + 무한 캔버스

```
┌─────────────────────────────────────────────────────────────────┐
│  Navbar (64px) - Dark Theme                                     │
├─────────────────────────────────────────────────────────────────┤
│  Tabs: [📊 Canvas] [💬 Chat] [📁 History] [⚙️ Settings]        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Infinite Canvas (전체 화면)                                    │
│                                                                 │
│    [생성 이미지]    [텍스트]    [차트]    [위젯]               │
│       드래그          복사        이동      확대                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
│  Footer (40px) - Dark Theme                                     │
└─────────────────────────────────────────────────────────────────┘
```

**장점:**
- ✅ 생성 결과 영역 최대화 (100%)
- ✅ Chat/History는 탭으로 분리
- ✅ 가장 넓은 작업 공간
- ❌ 현재 구조에서 대폭 변경 필요

---

## 🛠️ 구현 계획 (Option A 기준)

### Phase 1: AI Chat 접기 기능 (1-2시간)
**목표**: AI Conversation 영역을 48px로 축소 가능하게

```css
/* AI Chat 접힘 상태 */
.center-column.collapsed {
    width: 48px !important;
}

.center-column.collapsed .conversation-area {
    display: none;
}

.center-column.collapsed .input-area {
    display: none;
}
```

**기능:**
- 접기/펼치기 버튼 추가
- 접힌 상태: 48px (아이콘만)
- 펼친 상태: 360px (현재보다 작게)
- 로컬 스토리지에 상태 저장

**효과:**
- AI Chat 접으면 Right 영역 +360px 증가

---

### Phase 2: 레이아웃 비율 조정 (1시간)
**목표**: 생성 결과 영역 확대

```css
/* 현재 */
.canvas-container {
    grid-template-columns: 300px 1fr 400px;
}

/* 개선 */
.canvas-container {
    grid-template-columns: 280px 360px 1fr;
}
```

**변경:**
- Left: 300px → 280px (-20px)
- Center (AI): 1fr → 360px (고정)
- Right (Canvas): 400px → 1fr (가변, 최대)

**효과:**
- Right 영역 400px → 60-70% (1200px+)
- 생성 결과 공간 3배 증가

---

### Phase 3: 여백 최적화 (30분)
**목표**: 여백 30-40% 축소로 정보 밀도 증가

```css
/* 현재 → 개선 */
padding: 1.5rem → 0.75rem (50% 축소)
gap: 1rem → 0.5rem (50% 축소)
margin: 1.5rem → 0.75rem (50% 축소)
font-size: 16px → 14px (14% 축소)
line-height: 1.5 → 1.4 (7% 축소)
```

**적용 영역:**
- 모든 column-header
- conversation-area
- preview-panel
- widget-card
- message 요소

**효과:**
- 화면에 50% 더 많은 정보 표시
- 스크롤 없이 더 많은 내용 확인

---

### Phase 4: 무한 캔버스 기본 구조 (2-3시간)
**목표**: Right 영역을 무한 캔버스로 전환

**HTML 구조:**
```html
<div class="infinite-canvas-container">
    <!-- Canvas Controls -->
    <div class="canvas-controls">
        <button class="zoom-in">🔍+</button>
        <span class="zoom-level">100%</span>
        <button class="zoom-out">🔍-</button>
        <button class="reset-view">⟲</button>
        <button class="grid-toggle">⊞</button>
    </div>
    
    <!-- Canvas Area -->
    <div class="infinite-canvas" id="infiniteCanvas">
        <div class="canvas-grid"></div>
        <div class="canvas-items" id="canvasItems">
            <!-- 생성된 아이템들 -->
        </div>
    </div>
</div>
```

**기능:**
1. **Panning (이동)**:
   - 마우스 드래그로 캔버스 이동
   - 중간 버튼 또는 Space + 드래그

2. **Zooming (확대/축소)**:
   - 마우스 휠로 확대/축소
   - 범위: 25% ~ 300%
   - Ctrl + Wheel로 부드러운 줌

3. **Grid System**:
   - 20px 그리드 표시
   - 스냅 기능 (아이템 정렬)
   - 가이드 라인

**CSS:**
```css
.infinite-canvas {
    width: 100%;
    height: 100%;
    overflow: hidden;
    position: relative;
    cursor: grab;
    background: #F9FAFB;
}

.infinite-canvas.panning {
    cursor: grabbing;
}

.canvas-grid {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: 
        linear-gradient(rgba(0,0,0,0.05) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0,0,0,0.05) 1px, transparent 1px);
    background-size: 20px 20px;
}
```

---

### Phase 5: 아이템 관리 (2-3시간)
**목표**: 생성된 콘텐츠를 캔버스에 배치/관리

**아이템 타입:**
1. **이미지 카드**: 생성된 이미지
2. **텍스트 카드**: AI 응답 텍스트
3. **차트 카드**: 데이터 시각화
4. **위젯 카드**: 추가한 위젯

**아이템 기능:**
```javascript
class CanvasItem {
    constructor(type, content, x, y) {
        this.id = generateId();
        this.type = type; // 'image', 'text', 'chart', 'widget'
        this.content = content;
        this.x = x;
        this.y = y;
        this.width = 300;
        this.height = 200;
        this.zIndex = Date.now();
    }
    
    // 드래그
    startDrag() { }
    
    // 크기 조정
    resize(width, height) { }
    
    // 복사
    duplicate() { }
    
    // 삭제
    delete() { }
    
    // 저장
    save() { }
}
```

**HTML 템플릿:**
```html
<div class="canvas-item" data-id="123" style="left: 100px; top: 100px;">
    <div class="canvas-item-header">
        <span class="canvas-item-title">생성 이미지</span>
        <div class="canvas-item-actions">
            <button class="duplicate-btn">📋</button>
            <button class="delete-btn">🗑️</button>
        </div>
    </div>
    <div class="canvas-item-content">
        <img src="..." alt="Generated">
    </div>
    <div class="canvas-item-resize"></div>
</div>
```

---

### Phase 6: 저장/내보내기 (1-2시간)
**목표**: 캔버스 상태 저장 및 내보내기

**기능:**
1. **자동 저장**: 
   - LocalStorage에 5초마다 자동 저장
   - 최근 10개 버전 유지

2. **수동 저장**:
   - "저장" 버튼으로 명시적 저장
   - 프로젝트 이름 지정

3. **내보내기**:
   - PNG: 캔버스 이미지로 저장
   - PDF: 프레젠테이션용
   - JSON: 데이터 백업

```javascript
// 저장
function saveCanvas() {
    const canvas = {
        version: '1.0',
        timestamp: Date.now(),
        zoom: currentZoom,
        pan: { x: panX, y: panY },
        items: canvasItems.map(item => item.toJSON())
    };
    
    localStorage.setItem('canvas-v4-state', JSON.stringify(canvas));
}

// 불러오기
function loadCanvas() {
    const saved = localStorage.getItem('canvas-v4-state');
    if (saved) {
        const canvas = JSON.parse(saved);
        restoreCanvas(canvas);
    }
}

// PNG 내보내기
function exportToPNG() {
    html2canvas(document.getElementById('infiniteCanvas'))
        .then(canvas => {
            const link = document.createElement('a');
            link.download = 'museflow-canvas.png';
            link.href = canvas.toDataURL();
            link.click();
        });
}
```

---

## 📊 예상 효과

### 공간 활용
| 영역 | 현재 | 개선 후 | 증가율 |
|------|------|---------|--------|
| Left | 300px | 280px | -7% |
| AI Chat | ~50% | 360px (접으면 48px) | -60% |
| Canvas | 400px | ~60-70% | +300% |
| 여백 | 100% | 60-70% | -30% |
| 정보량 | 100% | 150% | +50% |

### 작업 효율
- ✅ 생성 결과 3배 더 넓게 표시
- ✅ 이미지/텍스트 동시에 여러 개 확인
- ✅ 드래그로 직관적 배치
- ✅ 확대/축소로 세밀한 작업
- ✅ 스크롤 없이 전체 내용 파악

---

## 🚀 실행 계획

### 단계별 소요 시간
- **Phase 1**: AI Chat 접기 (1-2시간)
- **Phase 2**: 레이아웃 조정 (1시간)
- **Phase 3**: 여백 최적화 (30분)
- **Phase 4**: 무한 캔버스 기본 (2-3시간)
- **Phase 5**: 아이템 관리 (2-3시간)
- **Phase 6**: 저장/내보내기 (1-2시간)

**총 소요 시간**: 8-12시간

### 권장 진행 방식
**Option 1: 점진적 개선 (추천)**
```
Day 1: Phase 1-3 (AI Chat 접기 + 레이아웃 + 여백) → 배포
→ 즉시 효과 확인, 피드백 수렴

Day 2: Phase 4 (무한 캔버스 기본) → 배포
→ 기본 기능 테스트

Day 3: Phase 5-6 (아이템 관리 + 저장) → 최종 배포
→ 완전한 기능 제공
```

**Option 2: 통합 개발**
```
1-2 Days: Phase 1-6 모두 개발 → 한 번에 배포
→ 완성도 높지만 피드백 늦음
```

---

## 🎨 디자인 세부사항

### 색상 팔레트 (현재 유지)
```css
--primary: #8B5CF6;
--background: #F9FAFB;
--card-bg: #FFFFFF;
--border: #E5E7EB;
--text-primary: #1F2937;
--text-secondary: #6B7280;
```

### 여백 축소 기준
```css
/* 헤더 */
.column-header { padding: 0.75rem; }  /* 1.5rem → 0.75rem */

/* 카드 */
.widget-card { padding: 0.75rem; }    /* 1rem → 0.75rem */
.message { padding: 0.75rem; }        /* 1rem → 0.75rem */

/* 간격 */
.gap-4 { gap: 0.5rem; }               /* 1rem → 0.5rem */
.mb-4 { margin-bottom: 0.5rem; }      /* 1rem → 0.5rem */

/* 폰트 */
body { font-size: 14px; }             /* 16px → 14px */
.column-title { font-size: 1rem; }    /* 1.125rem → 1rem */
```

---

## 🔍 현재 상태 보존

### Git 브랜치 전략
```bash
# 현재 상태 백업
git checkout -b canvas-v4-original

# 개선 작업용 브랜치
git checkout -b canvas-v4-layout-improvement

# 작업 후 비교
git diff canvas-v4-original canvas-v4-layout-improvement
```

### 롤백 가능성
- 각 Phase마다 커밋
- 문제 발생 시 이전 Phase로 복원 가능
- 원본 파일 백업: `canvas-v4-hybrid.html.backup`

---

## 📝 체크리스트

### Phase 1 (즉시 시작 가능)
- [ ] AI Chat 접기 버튼 추가
- [ ] 접힘/펼침 CSS 애니메이션
- [ ] 로컬 스토리지 상태 저장
- [ ] 테스트 및 배포

### Phase 2-3
- [ ] Grid 템플릿 변경
- [ ] 여백 축소 (padding, gap, margin)
- [ ] 폰트 크기 조정
- [ ] 반응형 확인

### Phase 4-6
- [ ] 무한 캔버스 HTML/CSS
- [ ] Panning/Zooming 구현
- [ ] Grid System
- [ ] 아이템 드래그
- [ ] 아이템 복사/삭제
- [ ] 저장/불러오기
- [ ] PNG/PDF 내보내기

---

## 🎯 최종 목표

**학예사가 가장 효율적으로 작업할 수 있는 캔버스**

1. ✅ 생성 결과를 크고 명확하게 확인
2. ✅ 여러 콘텐츠를 동시에 비교
3. ✅ 자유롭게 배치하고 조직화
4. ✅ 확대/축소로 세밀한 작업
5. ✅ 빠른 복사/붙여넣기로 재사용
6. ✅ 저장/내보내기로 결과물 공유

---

**작성일**: 2025-12-04  
**버전**: 2.0  
**상태**: 승인 대기
